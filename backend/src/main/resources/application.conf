pill {
  mode = "dev"
  mode = ${?MODE}

  db {
    url = "jdbc:mysql://localhost:3306/pill"
    url = ${?DB_URL}
    user = "changeme"
    user = ${?DB_USER}
    pass = "changeme"
    pass = ${?DB_PASS}
    migrateOnStart = false
  }

  push {
    apns {
      privateKey = "changeme"
      privateKey = ${?APNS_PRIVATE_KEY}
    }
  }
}
